# ────────────────────────────────────────────────────────────────────────────
# CORE — works on Python 3.11+ (incl. Python 3.14)
# Versions kept minimal so pip can pick wheels for any modern Python.
# ────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9

# Database (PostgreSQL)
sqlalchemy>=2.0.34
psycopg2-binary>=2.9.9
alembic>=1.13.2

# Validation & config
pydantic>=2.9.2
pydantic-settings>=2.5.2
email-validator>=2.2.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<5.0    # passlib 1.7.4 is incompatible with bcrypt>=5

# HTTP & async I/O
httpx>=0.27.2
aiofiles>=24.1.0

# Background scheduler (reminder notifications)
apscheduler>=3.10.4

# AI providers
groq>=0.11.0
pillow>=10.4.0

# Optional SMS
twilio>=9.3.3
pypdf>=5.0.0
